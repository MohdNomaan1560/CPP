#include <bits/stdc++.h>
  using namespace std;
  int main()
  {
    int x,r=0;
    cin>>x;
    while(x!=0){
      //x=x/10;
        r=r*10+x%10;
          x=x/10;
    }
    cout<<r;
    return 0;
  }
